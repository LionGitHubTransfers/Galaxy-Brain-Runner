using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BrainScaulpValueControlling : MonoBehaviour
{
    private void LateUpdate()
    {
        //transform.gameObject.GetComponent<SkinnedMeshRenderer>().SetBlendShapeWeight(0,FindObjectOfType<PlayerController>().blendShaphValue + 11);
    }
}
